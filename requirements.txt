# SDD Plugin — Python dependencies for multi-format export
# Install: pip install -r requirements.txt

python-docx>=0.8.11     # Word (.docx) generation
openpyxl>=3.1.2         # Excel (.xlsx) generation
python-pptx>=0.6.21     # PowerPoint (.pptx) generation
